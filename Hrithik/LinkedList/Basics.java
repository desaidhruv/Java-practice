
public class Basics {
    Node head; // Head of the list

    // Linkded List Node
    static class Node {
        int data;
        Node next;
    // Constructor to create a new node 
    // Next is by default intitalized as null
        Node(int d) {
            data = d;
        }
    }
}